
let answers = {
    'acquaintance': 'a person that you have met but do not know well',
    'to acquire': 'to get something',
    'acquaintance': 'something other',
}




for (const key in answers) {
    if (answers.hasOwnProperty(key)) {
        const element = answers[key];
    }
}

